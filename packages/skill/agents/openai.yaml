display_name: AgentPay
short_description: Owner-authorized cross-chain payments for AI agents.
default_prompt: Use AgentPay to create an agent wallet, prepare an owner-signed X Layer payment, open the Review & Sign URL, retrieve the verified EIP-712 signature, execute via the paid MCP ASP, and track status.
